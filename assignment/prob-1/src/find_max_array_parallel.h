#ifndef FIND_MAX_ARRAY_PARALLEL_H
#define FIND_MAX_ARRAY_PARALLEL_H 

void init (int * a, int size);

int find_max_sequence (int * a, int size);

int find_max_parallel (int * a, int size);

int get_max(int a, int b);

#endif

